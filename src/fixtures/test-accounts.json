{
  "$comment": [
    "THE central registry of accounts the bench uses. Specs resolve accounts by ROLE through",
    "src/fixtures/test-accounts.ts and never hard-code a KPost ID.",
    "",
    "These are the PRE-EXISTING accounts declared in .env, not accounts the bench created. The",
    "qatest_* provisioning attempt was abandoned for a product reason, recorded in blockedByProduct",
    "below: KPost rejects any kpostID containing an underscore, so the mandated qatest_<name> shape",
    "cannot be registered through the product at all.",
    "",
    "NO IDs OR PASSWORDS LIVE IN THIS FILE. Both are POINTERS to environment variables",
    "(kpostIdEnv / passwordEnv). The file is committed; .env is not."
  ],
  "domainPolicy": {
    "PERSONAL": "kpostindia.com",
    "BUSINESS_S": "kpost.in",
    "BUSINESS_M": "kpost.in",
    "BUSINESS_L": "kpost.in",
    "rationale": [
      "Verified against KPOST_QA (active accounts): PERSONAL @kpostindia.com 324, PERSONAL",
      "@kpost.in 205 (LEGACY, predates the split), BUSINESS_* @kpost.in 648.",
      "",
      "An account marked legacyDomain: true is a personal account on the business domain. It is",
      "pre-existing data, NOT a defect and NOT something to rewrite — kpost_id is the primary key",
      "across ~90 tables. The flag exists so the mismatch is declared rather than silently tolerated."
    ],
    "blockedByProduct": [
      "MEASURED 2026-09-21 against testingapi + KPOST_QA:",
      "Signup REJECTS any kpostID whose local part contains an UNDERSCORE, answering HTTP 200 with",
      "{statusCode: 500, message: \"Enter valid Credentials\"}. Decisive test: two otherwise identical",
      "payloads with distinct mobiles — qatest_sender@kpostindia.com refused, qatestsender@kpostindia.com",
      "not refused for that reason. Corroborated: 0 of 2,167 accounts on KPOST_QA contain an underscore."
    ]
  },
  "accounts": [
    {
      "id": "primary",
      "kpostIdEnv": "QA_KPOST_ID",
      "passwordEnv": "QA_PASSWORD",
      "userType": "PERSONAL",
      "legacyDomain": true,
      "purpose": "The bench's own account: sender in 1:1 flows and caller for profile reads. On the legacy @kpost.in domain.",
      "provisioned": true
    },
    {
      "id": "counterparty",
      "kpostIdEnv": "QA_VICTIM_KPOST_ID",
      "passwordEnv": "QA_PASSWORD",
      "userType": "PERSONAL",
      "purpose": "The receiver in 1:1 Katchup and KMail flows, and the second session for isolation probes.",
      "provisioned": true
    },
    {
      "id": "observer",
      "kpostIdEnv": "QA_PERSONAL_3_KPOST_ID",
      "passwordEnv": "QA_PASSWORD",
      "userType": "PERSONAL",
      "purpose": "A third party: Cc, confidential-copy invisibility (NFR-SEC02) and a group of three.",
      "provisioned": true
    },
    {
      "id": "company-admin",
      "kpostIdEnv": "QA_BUSINESS_M_KPOST_ID",
      "passwordEnv": "QA_PASSWORD",
      "userType": "BUSINESS_M",
      "companyIdEnv": "QA_BUSINESS_M_COMPANY_ID",
      "purpose": "Administrator of the BUSINESS_M company (Nebius, 242); drives the Admin permission workflows.",
      "provisioned": true
    },
    {
      "id": "company-member",
      "kpostIdEnv": "QA_BUSINESS_M_USER_1_KPOST_ID",
      "passwordEnv": "QA_PASSWORD",
      "userType": "BUSINESS_M",
      "companyIdEnv": "QA_BUSINESS_M_COMPANY_ID",
      "purpose": "An ordinary member of that company: the subject of permission and role checks.",
      "provisioned": true
    },
    {
      "id": "company-second-member",
      "kpostIdEnv": "QA_BUSINESS_M_USER_2_KPOST_ID",
      "passwordEnv": "QA_PASSWORD",
      "userType": "BUSINESS_M",
      "companyIdEnv": "QA_BUSINESS_M_COMPANY_ID",
      "purpose": "The third member, so a company of three can be exercised. NEVER terminated — the Admin DB is live-adjacent and members cannot be recreated via API.",
      "provisioned": true
    }
  ]
}
