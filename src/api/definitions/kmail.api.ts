/**
 * KMail is registered from `./kmail/index` (suite `kmail-api`, host kmail5). It was previously an
 * empty stub while the module was out of scope; now built. See `docs/kmail-flow.md`.
 */
export { kmailApis } from './kmail/index';
